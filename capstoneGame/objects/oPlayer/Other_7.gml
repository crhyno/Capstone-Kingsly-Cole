//Checks to see specific states at the end of animations and sets state back to move
if (state == "attack one" || state == "attack two" || state == "attack three" || state == "roll") {
	state = "move";
}
